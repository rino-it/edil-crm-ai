Contesto

Contesto
Le scadenze importate dal file Pivot sono state inserite nel database con valori errati:

Stato sbagliato: tutte le righe con Sdo=x sono finite come stato='pagato' con importo_pagato=importo_totale → residuo zero → escluse dal cashflow
Campo sbagliato: la stringa "pivot" è finita in note anziché in descrizione (che risulta vuoto)
Date nel passato: molte scadenze hanno date 2024-2025, finendo nel pregresso

Azione 1 — Rollback SQL
Eseguire in Supabase SQL Editor:
sqlDELETE FROM scadenze_pagamento WHERE note ILIKE '%pivot%';
Verifica prima con un count:
sqlSELECT COUNT(*) FROM scadenze_pagamento WHERE note ILIKE '%pivot%';

Azione 2 — Fix script scripts/import_pivot_cashflow.py
2.1 — Forzare stato = 'da_pagare' e importo_pagato = 0 all'inserimento
Problema: La logica attuale (righe 260-273) legge la colonna Sdo e marca le righe come "pagato".
Soluzione: Per lo scopo del cashflow previsionale, tutte le righe devono entrare come da_pagare con importo_pagato = 0. Lo storico dei pagamenti effettivi non serve per la proiezione.
Sostituire tutto il blocco calcolo stato (righe 260-273):
python        # ── Calcolo stato ──────────────────────────────────────────
        if sdo in ("x", "X"):
            stato          = "pagato"
            importo_pagato = abs(importo_pagato_csv) if importo_pagato_csv else abs(importo)
        elif is_nc:
            # Nota di credito → considerata chiusa
            stato          = "pagato"
            importo_pagato = abs(importo)
        elif data_scadenza < OGGI:
            stato          = "scaduto"
            importo_pagato = abs(importo_pagato_csv)
        else:
            stato          = "da_pagare"
            importo_pagato = abs(importo_pagato_csv)
Con:
python        # ── Stato per cashflow previsionale ───────────────────────
        # Note di credito → chiuse (non servono nel cashflow)
        if is_nc:
            stato          = "pagato"
            importo_pagato = abs(importo)
        else:
            # TUTTE le altre righe → da_pagare con importo_pagato=0
            # Il cashflow proietta il residuo (importo_totale - importo_pagato)
            stato          = "da_pagare"
            importo_pagato = 0
2.2 — Spostare "Schedulazione Pivot" nel campo descrizione
Problema: Riga 285 mette tutto in note:
pythonnote_completa = " | ".join(x for x in ["pivot", appunti, note] if x).strip(" |")
E nel record (righe 287-301) il campo descrizione non è presente.
Soluzione: Aggiungere il campo descrizione al record, con formato "Schedulazione Pivot: {fornitore}".
Sostituire riga 285:
python        note_completa = " | ".join(x for x in ["pivot", appunti, note] if x).strip(" |")
Con:
python        # Descrizione per identificare la sorgente
        descrizione_pivot = f"Schedulazione Pivot: {fornitore_raw}"
        # Note: solo appunti e note operative (senza "pivot" hardcoded)
        note_completa = " | ".join(x for x in [appunti, note] if x).strip(" |") or None
E nel dizionario record (righe 287-301), aggiungere "descrizione" e aggiornare "note":
Sostituire:
python            "metodo_pagamento":    modalita or None,
            "note":                note_completa or None,
Con:
python            "metodo_pagamento":    modalita or None,
            "descrizione":         descrizione_pivot,
            "note":                note_completa,
2.3 — Fix parsing date: anno futuro se manca o è passato
Problema: get_date() (righe 112-122) gestisce solo formati completi con anno (%d/%m/%Y, ecc.). Se il CSV avesse date senza anno (es. 28/02), restituisce None e il fallback è data_emissione o OGGI.
Inoltre le date 2024/2025 sono tutte nel passato.
Soluzione: Modificare get_date() per:

Supportare il formato dd/mm (senza anno) → assegnare anno corrente o prossimo futuro
Aggiungere un parametro opzionale force_future=False da usare per la colonna Scadenza

Sostituire la funzione get_date (righe 112-122):
pythondef get_date(s, force_future: bool = False) -> str | None:
    """Prova vari formati data → ISO string o None.
    Se force_future=True e la data è nel passato, sposta all'anno prossimo.
    Supporta anche formato dd/mm senza anno."""
    if not s:
        return None
    s = str(s).strip()

    parsed = None
    # Prova formati con anno
    for fmt in ("%d/%m/%Y", "%Y-%m-%d", "%d-%m-%Y", "%d.%m.%Y"):
        try:
            parsed = datetime.strptime(s, fmt).date()
            break
        except ValueError:
            continue

    # Prova formato senza anno: dd/mm
    if parsed is None:
        for fmt in ("%d/%m", "%d-%m"):
            try:
                parsed = datetime.strptime(s, fmt).date().replace(year=date.today().year)
                break
            except ValueError:
                continue

    if parsed is None:
        return None

    # Se force_future e la data è nel passato, sposta all'anno prossimo
    if force_future and parsed < date.today():
        parsed = parsed.replace(year=date.today().year + 1)

    return parsed.isoformat()
E nelle chiamate (righe 249-250), usare force_future=True per la colonna Scadenza:
Sostituire:
python        data_emissione = get_date(row.get(COL_DATA_EMI, ""))
        data_scadenza  = get_date(row.get(COL_SCADENZA, "")) or data_emissione or OGGI
Con:
python        data_emissione = get_date(row.get(COL_DATA_EMI, ""))
        data_scadenza  = get_date(row.get(COL_SCADENZA, ""), force_future=True) or data_emissione or OGGI

File da modificare
#FileModifica1scripts/import_pivot_cashflow.py3 fix: stato da_pagare, descrizione pivot, date future
Verifica

Eseguire SQL rollback → verificare SELECT COUNT(*) FROM scadenze_pagamento WHERE note ILIKE '%pivot%' → 0
python scripts/import_pivot_cashflow.py --dry-run → tutte le righe con stato da_pagare, importo_pagato 0
python scripts/import_pivot_cashflow.py → import ok
In Supabase: SELECT stato, importo_pagato, descrizione, note FROM scadenze_pagamento WHERE descrizione ILIKE '%pivot%' LIMIT 5 → stato=da_pagare, importo_pagato=0, descrizione="Schedulazione Pivot: XYZ", note contiene solo appunti operativi
/finanza/programmazione → le scadenze pivot compaiono nel cashflow con date future
/finanza → grafico SVG mostra l'impatto delle scadenze pivot sul saldo


step 2
Le funzionalità base (KPI residuo, chart senza soglia, import CSV pivot, cashflow settimanale espandibile) sono operative. Ora implementiamo le 3 fasi evolutive per sostituire completamente il workflow Excel:



Fase 1: Costi fissi ricorrenti (mutui, stipendi, domiciliazioni) che oggi mancano dal cashflow

Fase 2: Workflow approvazione fatture (da_verificare → da_pagare) + split in rate (accordi)

Fase 3: Programmazione interattiva con drag & drop + allarmi saldo negativo

FASE 0 — Azione Operativa: Consolidamento Dati

Prerequisito da completare prima delle Fasi 1-3. Allinea il database alla programmazione finanziaria reale.

0.1 — Completare caricamento XML arretrati

Dalla webapp /finanza/importa-fatture, caricare tutti gli XML di vendita/acquisto arretrati che mancano ancora nel DB.



0.2 — Creare anagrafiche "senza fattura" nella webapp

In /anagrafiche, creare i soggetti che non hanno fatture XML ma rappresentano costi fissi/ricorrenti:

Soggetto da creareTipoNoteMutuo BCCFornitore/BancaRate mutuo bancarioEdilcassaEnteContributi cassa edileStipendi (o nome specifico)InternoCosto personale(altri da confermare)

L'utente deve confermare i nomi esatti che ha già creato o creerà, perché serviranno nel passo successivo.



0.3 — Aggiungere ALIAS_FORNITORI allo script import_pivot_cashflow.py

Il CSV pivot contiene nomi fornitori che non matchano al 100% con l'anagrafica Supabase. Il dizionario ALIAS risolve queste discrepanze prima del fuzzy match.

Aggiungere nel file scripts/import_pivot_cashflow.py dopo le costanti COL_* (riga ~50) e prima degli HELPERS:





python



# ─── ALIAS FORNITORI ──────────────────────────────────────────────────────────# Mappa: nome_csv (UPPERCASE) → nome_anagrafica (UPPERCASE, come in Supabase)# Usato PRIMA del fuzzy match per risolvere casi certi.ALIAS_FORNITORI: dict[str, str] = {    # Esempio — l'utente deve inserire i nomi esatti:    # "MUTUO BCC":          "MUTUO BCC",    # "EDILCASSA":           "EDILCASSA",    # "STIPENDI":            "STIPENDI DIPENDENTI",    # "ROSSI SRL":           "ROSSI S.R.L.",    # "BIANCHI & C":         "BIANCHI E C. SAS",}

Modificare la funzione match_soggetto() per consultare ALIAS prima del fuzzy:





python



def match_soggetto(nome_csv: str, dizionario: dict[str, str]) -> str | None:    """Match con alias espliciti + fallback fuzzy match (soglia 85)."""    nome_upper = norm(nome_csv).upper()    if not nome_upper:        return None    # 1. Check alias esplicito    if nome_upper in ALIAS_FORNITORI:        alias_target = ALIAS_FORNITORI[nome_upper]        if alias_target in dizionario:            return dizionario[alias_target]    # 2. Match diretto    if nome_upper in dizionario:        return dizionario[nome_upper]    # 3. Fuzzy fallback    try:        from thefuzz import process    except ImportError:        return None    risultato = process.extractOne(nome_upper, list(dizionario.keys()), score_cutoff=85)    return dizionario[risultato[0]] if risultato else None

0.4 — Lanciare import definitivo





bash



# 1. Prima dry-run per verificare che tutti i soggetti matchinopython scripts/import_pivot_cashflow.py --dry-run# 2. Controllare "Soggetti non trovati" → aggiungere alias mancanti# 3. Ripetere dry-run fino a 0 mancanti# 4. Import definitivopython scripts/import_pivot_cashflow.py

Dopo l'import:



/finanza/programmazione → le fatture sono distribuite nelle settimane corrette

/finanza → KPI riflettono i dati reali

FASE 1 — Costi Fissi Ricorrenti

1.1 — Migrazione DB: tabella costi_fissi_ricorrenti

Eseguire in Supabase SQL Editor:





sql



CREATE TABLE costi_fissi_ricorrenti (  id UUID DEFAULT gen_random_uuid() PRIMARY KEY,  descrizione TEXT NOT NULL,  importo NUMERIC(12,2) NOT NULL,  tipo TEXT NOT NULL DEFAULT 'uscita' CHECK (tipo IN ('entrata', 'uscita')),  frequenza TEXT NOT NULL DEFAULT 'mensile' CHECK (frequenza IN ('settimanale', 'mensile', 'trimestrale', 'annuale')),  giorno_mese INTEGER DEFAULT 1 CHECK (giorno_mese BETWEEN 1 AND 31),  conto_id UUID REFERENCES conti_banca(id),  categoria TEXT,  attivo BOOLEAN DEFAULT true,  data_inizio DATE DEFAULT CURRENT_DATE,  data_fine DATE,  note TEXT,  created_at TIMESTAMPTZ DEFAULT now(),  updated_at TIMESTAMPTZ DEFAULT now());ALTER TABLE costi_fissi_ricorrenti ENABLE ROW LEVEL SECURITY;CREATE POLICY "authenticated_access" ON costi_fissi_ricorrenti  FOR ALL TO authenticated USING (true) WITH CHECK (true);

1.2 — utils/data-fetcher.ts: nuove funzioni

getCostiFissiRicorrenti() — CRUD read (dopo riga ~2293, prima della sezione RICONCILIAZIONE)





typescript



export interface CostoFisso {  id: string;  descrizione: string;  importo: number;  tipo: 'entrata' | 'uscita';  frequenza: 'settimanale' | 'mensile' | 'trimestrale' | 'annuale';  giorno_mese: number;  conto_id: string | null;  categoria: string | null;  attivo: boolean;  data_inizio: string;  data_fine: string | null;  note: string | null;}export async function getCostiFissiRicorrenti(): Promise<CostoFisso[]> {  const supabase = getSupabaseAdmin();  const { data } = await supabase    .from('costi_fissi_ricorrenti')    .select('*')    .eq('attivo', true)    .order('descrizione');  return (data || []) as CostoFisso[];}

generaCostiFissiPerPeriodo(giorni) — genera occorrenze future per il cashflow





typescript



export function generaCostiFissiPerPeriodo(  costi: CostoFisso[],  giorni: number): Array<{ data: string; importo: number; tipo: 'entrata' | 'uscita'; descrizione: string }> {  const oggi = new Date();  oggi.setHours(0,0,0,0);  const fine = addDays(oggi, giorni);  const occorrenze: Array<{data: string; importo: number; tipo: 'entrata'|'uscita'; descrizione: string}> = [];  for (const c of costi) {    if (!c.attivo) continue;    const inizio = new Date(c.data_inizio);    const dataFine = c.data_fine ? new Date(c.data_fine) : fine;    // Per ogni mese nel periodo, genera la data di occorrenza    let cursor = new Date(oggi.getFullYear(), oggi.getMonth(), c.giorno_mese);    if (cursor < oggi) cursor.setMonth(cursor.getMonth() + 1);    while (cursor <= fine && cursor <= dataFine) {      if (cursor >= inizio) {        occorrenze.push({          data: cursor.toISOString().split('T')[0],          importo: c.importo,          tipo: c.tipo,          descrizione: c.descrizione,        });      }      // Incrementa in base alla frequenza      if (c.frequenza === 'settimanale') cursor = addDays(cursor, 7);      else if (c.frequenza === 'mensile') cursor.setMonth(cursor.getMonth() + 1);      else if (c.frequenza === 'trimestrale') cursor.setMonth(cursor.getMonth() + 3);      else cursor.setFullYear(cursor.getFullYear() + 1);    }  }  return occorrenze;}

1.3 — Iniettare costi fissi in getCashflowPrevisionale() (riga 1192)

Dopo riga 1232 (dopo il ciclo future.forEach), aggiungere:





typescript



// Inietta costi fissi ricorrenticonst costiFissi = await getCostiFissiRicorrenti();const occorrenzeFisse = generaCostiFissiPerPeriodo(costiFissi, giorni);occorrenzeFisse.forEach(occ => {  if (!mappaGiorni[occ.data]) mappaGiorni[occ.data] = { entrate: 0, uscite: 0 };  if (occ.tipo === 'entrata') mappaGiorni[occ.data].entrate += occ.importo;  else mappaGiorni[occ.data].uscite += occ.importo;});

1.4 — Iniettare costi fissi in getCashflowProjection() (riga 2190)

Prima del ciclo safeScadenze.forEach (riga 2231), aggiungere:





typescript



// Inietta costi fissi nelle settimaneconst costiFissi = await getCostiFissiRicorrenti();const occorrenzeFisse = generaCostiFissiPerPeriodo(costiFissi, days);occorrenzeFisse.forEach(occ => {  const dOcc = new Date(occ.data);  const ws = startOfWeek(dOcc, { weekStartsOn: 1 });  const we = endOfWeek(ws, { weekStartsOn: 1 });  const label = `Dal ${format(ws, 'dd/MM')} al ${format(we, 'dd/MM')}`;  if (weeksMap.has(label)) {    const bucket = weeksMap.get(label)!;    if (occ.tipo === 'entrata') bucket.entrate += occ.importo;    else bucket.uscite += occ.importo;    bucket.dettagli.push({      ragione_sociale: `📌 ${occ.descrizione}`,      fattura_riferimento: 'Costo fisso',      data_effettiva: occ.data,      importo_residuo: occ.importo,      tipo: occ.tipo,    });  }});```### 1.5 — Nuova pagina CRUD: `app/finanza/costi-fissi/`**`app/finanza/costi-fissi/page.tsx`** — Server component (lista + dialog aggiunta)```Struttura:- Card header con titolo + bottone "Aggiungi Costo Fisso"- Tabella: Descrizione | Importo | Tipo | Frequenza | Giorno | Conto | Categoria | Attivo | Azioni- Badge colorati per tipo (entrata verde, uscita rosso)- Toggle attivo/disattivo inline- Pulsanti Modifica/Elimina per riga

Usa getCostiFissiRicorrenti() + getContiBanca() (quest'ultima esiste già, riga ~1401 di data-fetcher.ts).

app/finanza/costi-fissi/actions.ts — Server actions CRUD





typescript



// creaCostoFisso(formData) → insert in costi_fissi_ricorrenti// aggiornaCostoFisso(id, data) → update// eliminaCostoFisso(id) → update attivo=false (soft delete)// toggleCostoFisso(id, attivo) → toggle attivo```Ogni action chiama `revalidatePath('/finanza/costi-fissi')` + `revalidatePath('/finanza')` + `revalidatePath('/finanza/programmazione')`.**`app/finanza/costi-fissi/AggiungiCostoDialog.tsx`** — Client component dialog```Campi form:- descrizione (text input, required)- importo (number input, required)- tipo (select: entrata/uscita, default uscita)- frequenza (select: settimanale/mensile/trimestrale/annuale, default mensile)- giorno_mese (number 1-31, default 1)- conto_id (select da lista conti banca)- categoria (text input, opzionale: "Mutuo", "Stipendio", "Utenza", "Leasing", "Assicurazione")- note (textarea, opzionale)

Usa shadcn Dialog + form pattern identico ad altre pagine del progetto.



1.6 — Sidebar: aggiungere link "Costi Fissi"

In components/Sidebar.tsx, sotto la voce "Programmazione" (riga 134), aggiungere:





tsx



import { Repeat } from 'lucide-react'// ...<SubMenuLink href="/finanza/costi-fissi" isActive={pathname === '/finanza/costi-fissi'} icon={<Repeat size={14} className="text-amber-400" />}>  Costi Fissi</SubMenuLink>

FASE 2 — Workflow Approvazione + Split Rate

2.1 — Nuovo stato da_verificare

Aggiungere il valore al CHECK constraint esistente su scadenze_pagamento.stato:





sql



ALTER TABLE scadenze_pagamento DROP CONSTRAINT IF EXISTS scadenze_pagamento_stato_check;ALTER TABLE scadenze_pagamento ADD CONSTRAINT scadenze_pagamento_stato_check  CHECK (stato IN ('da_pagare', 'parziale', 'pagato', 'scaduto', 'in_contestazione', 'da_verificare'));

2.2 — Escludere da_verificare da cashflow e KPI

Aggiungere .neq('stato', 'da_verificare') in 3 punti di utils/data-fetcher.ts:

FunzioneRiga approssimativaModificagetKPIFinanziariGlob()~1060Aggiungere .neq('stato', 'da_verificare') dopo .neq('stato', 'pagato')getCashflowPrevisionale()~1208Aggiungere .neq('stato', 'da_verificare') dopo .neq('stato', 'pagato')getCashflowProjection()~2206Aggiungere .neq('stato', 'da_verificare') dopo .neq('stato', 'pagato')

2.3 — API count: app/api/cron/scadenze/count-da-verificare/route.ts

Stesso pattern di count-da-smistare (già esistente):





typescript



import { NextResponse } from 'next/server';import { createClient } from '@/utils/supabase/server';export const dynamic = 'force-dynamic';export async function GET() {  const supabase = await createClient();  const { count, error } = await supabase    .from('scadenze_pagamento')    .select('*', { count: 'exact', head: true })    .eq('stato', 'da_verificare');  if (error) return NextResponse.json({ count: 0 });  return NextResponse.json({ count });}

2.4 — Sidebar: aggiungere "Da Verificare" con badge

In components/Sidebar.tsx:



Aggiungere hook useVerificaCount() identico a useSmistareCount(), che chiama /api/cron/scadenze/count-da-verificare

Aggiungere import { ShieldCheck } da lucide-react

Aggiungere link sotto "Da Smistare" (riga ~114):





tsx



<SubMenuLink href="/scadenze/da-verificare" isActive={pathname === '/scadenze/da-verificare'} icon={<ShieldCheck size={14} className="text-yellow-500" />} badge={verificaCount}>  Da Verificare</SubMenuLink>```### 2.5 — Pagina Approvazione: `app/scadenze/da-verificare/`**`app/scadenze/da-verificare/page.tsx`** — Server component```Struttura:- Titolo "Fatture Da Verificare" con icona ShieldCheck- Alert informativo: "Queste fatture sono in attesa di approvazione. Non influenzano il cashflow fino all'approvazione."- Tabella con colonne: Checkbox | Fornitore | Fattura | Importo | Data Scadenza | Data Emissione | Azioni- Barra azioni top: "Approva Selezionate" (bulk) + "Rifiuta Selezionate"- Per ogni riga: bottoni Approva (→ da_pagare) | Rifiuta (→ elimina o in_contestazione) | Split in Rate

Fetch: supabase.from('scadenze_pagamento').select('*, anagrafica_soggetti(ragione_sociale)').eq('stato', 'da_verificare').order('data_scadenza')

app/scadenze/da-verificare/actions.ts — Server actions





typescript



// approvaScadenza(id) → update stato='da_pagare', revalidate// approvaBulk(ids[]) → update bulk, revalidate// rifiutaScadenza(id) → update stato='in_contestazione' o delete, revalidate```### 2.6 — Split Fattura in Rate: `SplitRateDialog.tsx`**`app/scadenze/da-verificare/SplitRateDialog.tsx`** — Client component```Dialog modale con:- Info fattura originale: fornitore, nr fattura, importo totale- Numero rate (input number, min 2, max 12)- Tabella generata automaticamente:  | Rata # | Importo (auto: totale/n_rate) | Data Scadenza (auto: +30gg incrementale) |  | Modificabile inline | Modificabile inline |- Validazione: somma rate == importo originale- Conferma → chiama splitScadenzaInRate()

Server action splitScadenzaInRate() in app/scadenze/actions.ts:





typescript



export async function splitScadenzaInRate(  scadenzaOrigId: string,  rate: Array<{ importo: number; data_scadenza: string }>) {  const supabase = await createClient();  // 1. Leggi scadenza originale  const { data: orig } = await supabase    .from('scadenze_pagamento')    .select('*')    .eq('id', scadenzaOrigId)    .single();  if (!orig) throw new Error('Scadenza non trovata');  // 2. Crea N nuove scadenze (una per rata)  const nuoveRate = rate.map((r, i) => ({    soggetto_id: orig.soggetto_id,    cantiere_id: orig.cantiere_id,    tipo: orig.tipo,    fattura_riferimento: `${orig.fattura_riferimento || ''} (${i+1}/${rate.length})`,    importo_totale: r.importo,    importo_pagato: 0,    data_emissione: orig.data_emissione,    data_scadenza: r.data_scadenza,    data_pianificata: r.data_scadenza,    stato: 'da_pagare',    metodo_pagamento: orig.metodo_pagamento,    note: `Split da fattura ${orig.fattura_riferimento} - Rata ${i+1}/${rate.length}`,  }));  const { error: insertErr } = await supabase    .from('scadenze_pagamento')    .insert(nuoveRate);  if (insertErr) throw new Error(insertErr.message);  // 3. Marca originale come pagato/chiuso (è stata sostituita dalle rate)  await supabase    .from('scadenze_pagamento')    .update({ stato: 'pagato', note: `Sostituita da ${rate.length} rate split` })    .eq('id', scadenzaOrigId);  revalidatePath('/scadenze');  revalidatePath('/finanza');  revalidatePath('/finanza/programmazione');  return { success: true };}

FASE 3 — Programmazione Interattiva (Drag & Drop)

3.1 — Installare @dnd-kit





bash



npm install @dnd-kit/core @dnd-kit/sortable @dnd-kit/utilities

3.2 — utils/data-fetcher.ts: getFattureDaPianificare()

Nuova funzione per le fatture non ancora pianificate (senza data_pianificata o con stato scaduto):





typescript



export interface FatturaDaPianificare {  id: string;  ragione_sociale: string;  fattura_riferimento: string | null;  importo_residuo: number;  tipo: 'entrata' | 'uscita';  data_scadenza: string;  data_pianificata: string | null;  stato: string;}export async function getFattureDaPianificare(): Promise<FatturaDaPianificare[]> {  const supabase = getSupabaseAdmin();  const { data } = await supabase    .from('scadenze_pagamento')    .select(`      id, tipo, importo_totale, importo_pagato,      data_scadenza, data_pianificata, stato,      fattura_riferimento,      anagrafica_soggetti:soggetto_id (ragione_sociale)    `)    .in('stato', ['scaduto', 'da_pagare'])    .order('data_scadenza', { ascending: true });  return (data || []).map(s => ({    id: s.id,    ragione_sociale: (s as any).anagrafica_soggetti?.ragione_sociale || 'N/D',    fattura_riferimento: s.fattura_riferimento,    importo_residuo: Number(s.importo_totale) - Number(s.importo_pagato || 0),    tipo: s.tipo as 'entrata' | 'uscita',    data_scadenza: s.data_scadenza,    data_pianificata: s.data_pianificata,    stato: s.stato,  })).filter(s => s.importo_residuo > 0);}```### 3.3 — Rework pagina `app/finanza/programmazione/page.tsx`Struttura a 2 colonne:```┌─────────────────────────────────┐  ┌──────────────────────┐│  Alert Rischio (se negativo)    │  │                      │├─────────────────────────────────┤  │  SIDEBAR DESTRA      ││  KPI Card saldo iniziale        │  │  "Da Pianificare"    │├─────────────────────────────────┤  │                      ││  Chart Recharts (invariato)      │  │  Lista draggable     │├─────────────────────────────────┤  │  di fatture scadute  ││                                 │  │  o non pianificate   ││  Tabella Cashflow Settimanale   │  │                      ││  (settimane = drop target)      │  │  [fattura 1] drag    ││                                 │  │  [fattura 2] drag    ││  Week 1  ░░░░░░░░░░ drop here  │  │  [fattura 3] drag    ││  Week 2  ░░░░░░░░░░ drop here  │  │                      ││  Week 3  ░░░░░░░░░░ drop here  │  │                      ││                                 │  │                      │└─────────────────────────────────┘  └──────────────────────┘

La pagina server component fetcha:



getCashflowProjection(90) (già esistente)

getFattureDaPianificare() (nuova)

getCostiFissiRicorrenti() (nuova, Fase 1)

Poi passa tutto a <ProgrammazioneInterattiva>.



3.4 — app/finanza/programmazione/ProgrammazioneInterattiva.tsx

Client component wrapper con DndContext:





typescript



'use client'import { DndContext, DragOverlay, closestCenter } from '@dnd-kit/core'import { useState } from 'react'import { aggiornaDataPianificata } from './actions'  // Già esistente!import { FattureDaPianificareList } from './FattureDaPianificareList'import { CashflowTableDnd } from './CashflowTableDnd'// Gestisce:// - onDragStart: mostra DragOverlay con info fattura// - onDragEnd: chiama aggiornaDataPianificata(fattura.id, weekStart) + router.refresh()// - State: activeDrag, simulazione what-if temporanea```### 3.5 — `app/finanza/programmazione/FattureDaPianificareList.tsx`**Client component**: lista laterale di fatture draggabili.```Ogni item mostra:- Badge IN/OUT colorato- Ragione sociale (bold)- Nr fattura (mono, piccolo)- Importo residuo- Data scadenza originale- Stato badge (scaduto = rosso, da_pagare = arancione)Ogni item è wrappato con useDraggable() di @dnd-kit

3.6 — app/finanza/programmazione/CashflowTableDnd.tsx

Client component: versione della CashflowTable con drop targets.

Estende CashflowTable.tsx esistente aggiungendo:



Ogni riga settimana è un useDroppable() con id = weekStart ISO

Highlight verde quando un item viene trascinato sopra

Indicatore visuale: "Rilascia qui per pianificare nella settimana X"

Allarme saldo negativo: se saldoPrevisto < 0, la riga ha sfondo rosso + icona AlertTriangle + testo "Saldo negativo previsto!"

3.7 — Azione drag & drop

Riusa aggiornaDataPianificata() già esistente in app/finanza/programmazione/actions.ts.

Quando l'utente droppa una fattura su una settimana:



Chiama aggiornaDataPianificata(fattura.id, weekStartDate) (weekStart della settimana target)

router.refresh() per ricalcolare il cashflow server-side

La fattura sparisce dalla lista "Da Pianificare" e appare nella settimana target

Riepilogo File

File NUOVI da creare

#FileFaseDescrizione1app/finanza/costi-fissi/page.tsxF1Pagina CRUD costi fissi2app/finanza/costi-fissi/actions.tsF1Server actions CRUD3app/finanza/costi-fissi/AggiungiCostoDialog.tsxF1Dialog modale aggiunta/modifica4app/finanza/costi-fissi/CostiFissiTable.tsxF1Tabella client con toggle/delete5app/api/cron/scadenze/count-da-verificare/route.tsF2API count badge sidebar6app/scadenze/da-verificare/page.tsxF2Pagina approvazione fatture7app/scadenze/da-verificare/actions.tsF2Server actions approva/rifiuta8app/scadenze/da-verificare/SplitRateDialog.tsxF2Dialog split in rate9app/finanza/programmazione/ProgrammazioneInterattiva.tsxF3Wrapper DndContext10app/finanza/programmazione/FattureDaPianificareList.tsxF3Lista draggable sidebar11app/finanza/programmazione/CashflowTableDnd.tsxF3Tabella con drop targets

File ESISTENTI da modificare

#FileFaseModifica1scripts/import_pivot_cashflow.pyF0+ALIAS_FORNITORI dizionario, +logica alias in match_soggetto()2utils/data-fetcher.tsF1+F2+F3+CostoFisso interface, +getCostiFissiRicorrenti(), +generaCostiFissiPerPeriodo(), +getFattureDaPianificare(), inject costi fissi in getCashflowPrevisionale() e getCashflowProjection(), +.neq('da_verificare') in 3 query3components/Sidebar.tsxF1+F2+link "Costi Fissi" sotto Finanza, +link "Da Verificare" con badge sotto Scadenziario, +useVerificaCount hook, +import Repeat e ShieldCheck4app/finanza/programmazione/page.tsxF3Rework a 2 colonne, fetch getFattureDaPianificare(), passa a ProgrammazioneInterattiva5app/scadenze/actions.tsF2+splitScadenzaInRate() server action

Verifica

Fase 0

Anagrafiche "senza fattura" create in /anagrafiche (Mutuo BCC, Edilcassa, Stipendi, ecc.)

ALIAS_FORNITORI popolato con i nomi esatti

python scripts/import_pivot_cashflow.py --dry-run → 0 soggetti non trovati

python scripts/import_pivot_cashflow.py → import completo, cashflow riflette la programmazione reale

Fase 1

Creare tabella costi_fissi_ricorrenti in Supabase → OK senza errori

/finanza/costi-fissi → pagina carica, si possono aggiungere costi (es. "Mutuo BCC" €1.500/mese giorno 5)

/finanza/programmazione → nelle settimane future compaiono le voci "📌 Mutuo BCC" con importo corretto

/finanza → grafico SVG include impatto costi fissi sul saldo

Fase 2

Inserire manualmente 2-3 scadenze con stato='da_verificare' in Supabase

/scadenze/da-verificare → mostra le fatture da approvare

Approvare una fattura → stato diventa da_pagare, scompare dalla lista, appare nel cashflow

Split una fattura in 3 rate → originale diventa "pagato", 3 nuove scadenze con date diverse nel cashflow

Sidebar → badge "Da Verificare" mostra il count corretto

KPI Dashboard e Cashflow → NON includono fatture da_verificare

Fase 3

npm install @dnd-kit/core @dnd-kit/sortable @dnd-kit/utilities → OK

/finanza/programmazione → layout a 2 colonne con lista fatture a destra

Drag fattura dalla lista → drop su settimana → data_pianificata aggiornata, fattura appare nella settimana

Settimane con saldo negativo → sfondo rosso + allarme visivo

npm run build → nessun errore TypeScript

Cross-check finale

Il cashflow incluye sia scadenze reali che costi fissi ricorrenti

Le fatture da_verificare sono escluse ovunque (KPI, chart SVG, Recharts, tabella)

Le rate split compaiono nelle settimane corrette con data_pianificata individuale

Ordine di implementazione consigliato

Fase 0 prima — Consolidamento dati (ALIAS + import definitivo): prerequisito operativo

Fase 1 — Costi Fissi Ricorrenti (autocontenuta, nessuna dipendenza)

Fase 2 — Workflow Approvazione (il filtro da_verificare impatta le stesse query di F1)

Fase 3 ultima — Programmazione Interattiva (dipende dalle funzioni di F1 e F2 per dati completi)